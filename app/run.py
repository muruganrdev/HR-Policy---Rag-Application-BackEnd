# HR Policy RAG
# Run from the hr_policy_rag/ directory:
#   python -m uvicorn app.main:app --reload --port 8001

# Or from the parent directory:
#   uvicorn hr_policy_rag.app.main:app --reload --port 8001
